import jdk.incubator.jpackage.main.Main;

public class Exercico10 {
public static void main(String[] args) {

      int Number Vogais = contaVogais("E nois");
}
     static int contavogais(String Frase){

        String aux = Frase.toLowerCase(); 
        int cont = 0;

        for(int i = 0; i < aux.length(); i++){
            if (frase.chartAt( i ) == 'a' || 
            aux.charAt(i) == 'e' || 
            aux.charAt(i) == 'i'||  ) {
                
            }
            cont++;
        }



     }
}
